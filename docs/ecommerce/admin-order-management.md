# Admin order management

Admin can list/filter orders, open detail, transition status, set delivery tracking, add notes. Permissions: `orders.view`, `orders.update`, `orders.manage`.

Sidebar badges: awaiting payment, paid/processing counts via shared query helper.
