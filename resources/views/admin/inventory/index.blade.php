@extends('admin.layouts.app')
@section('title', 'Inventory')
@section('heading', 'Inventory')
@section('breadcrumb', 'Store / Inventory')
@section('content')
@if (session('status'))<p class="mb-4 border border-[var(--color-success)] bg-white px-4 py-3 text-[var(--color-success)]">{{ session('status') }}</p>@endif
<div class="grid gap-6 lg:grid-cols-[1.1fr,0.9fr]"><div class="admin-panel"><div class="admin-panel__head"><h2 class="admin-panel__title">Stock levels</h2></div><div class="admin-panel__body" style="padding:0;"><table class="admin-table"><thead><tr><th>Product</th><th>Stock</th><th>Threshold</th><th>Adjust</th></tr></thead><tbody>@foreach($products as $product)<tr><td>{{ $product->name }}</td><td>{{ $product->stock_quantity }}</td><td>{{ $product->low_stock_threshold }}</td><td><form method="POST" action="{{ route('admin.inventory.adjust', $product) }}" class="flex flex-wrap gap-2">@csrf <input type="number" name="quantity_change" class="admin-input" style="width:6rem;padding:0.45rem 0.55rem;" placeholder="+/-" required><input type="text" name="reason" class="admin-input" style="width:10rem;padding:0.45rem 0.55rem;" placeholder="Reason" required><button type="submit" class="btn btn-secondary">Save</button></form></td></tr>@endforeach</tbody></table></div></div><div class="admin-panel"><div class="admin-panel__head"><h2 class="admin-panel__title">Recent movements</h2></div><div class="admin-panel__body">@if($movements->isEmpty())<p class="admin-empty__copy">No movements recorded yet.</p>@else<ul class="admin-activity-list">@foreach($movements as $movement)<li class="admin-activity-list__item"><span class="admin-activity-list__time">{{ \Illuminate\Support\Carbon::parse($movement->created_at)->timezone(config('clinic.timezone'))->format('d M Y H:i') }}</span><span class="admin-activity-list__text">Product #{{ $movement->product_id }} · {{ $movement->quantity_change > 0 ? '+' : '' }}{{ $movement->quantity_change }} · {{ $movement->reason }}</span></li>@endforeach</ul>@endif</div></div></div>{{ $products->links() }}
@endsection
